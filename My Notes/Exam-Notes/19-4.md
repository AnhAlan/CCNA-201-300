- ipv6 có anycast còn ipv4 thì không
- lưu ý
    + switch access vlan 5
    + switch trunk allowed vlan 10,20
    + switch mode trunk
    + port đó sẽ hoạt động trunk do ta cấu hình switch mode trunk
    + switch access vlan 5 không có tác dụng
